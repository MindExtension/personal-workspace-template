name: Darbo erdvės šablono patikra

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Tikrinti Claude Code struktūrą ir privatumą
        run: bash scripts/check-public-template.sh
